spring:
  profiles:
    active: dev # Cambiar según el entorno
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/proaudio_channels?useSSL=false&serverTimezone=UTC
    username: root
    password: Maiescuel8042.
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: tu_correo@gmail.com
    password: tu_contraseña_de_aplicación
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  server:
    port: 8080

---

spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:mysql://localhost:3306/qrapp_test
    username: test_user
    password: test_password
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  server:
    port: 8081

---

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://db-servidor:3306/qrapp_prod
    username: prod_user
    password: prod_password
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  server:
    port: 80
